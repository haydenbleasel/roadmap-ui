---
title: Code Block
description: Provides syntax highlighting, line numbers, and copy to clipboard functionality for code blocks.
icon: Braces
---

import { AutoTypeTable } from 'fumadocs-typescript/ui';

<PoweredBy packages={[
]} />

<Preview name="code-block" code={`'use client';

import {
  CodeBlock,
  CodeBlockHeader,
  CodeBlockBody,
  CodeBlockCopyButton,
} from '@/components/ui/kibo-ui/code-block';

const code = \`function MyComponent(props) {
  return (
    <div>
      <h1>Hello, {props.name}!</h1>
      <p>This is an example React component.</p>
    </div>
  );
}\`;

const Example = () => (
  <div className="p-4">
    <CodeBlock>
      <CodeBlockHeader filename="MyComponent.js">
        <CodeBlockCopyButton value={code} />
      </CodeBlockHeader>
      <CodeBlockBody>{code}</CodeBlockBody>
    </CodeBlock>
  </div>
);

export default Example;`} />

## Installation

<Installer packageName="code-block" />

## Features

TBD.

## Examples

TBD.

## Props

The Calendar component is made up of the following subcomponents:

TBD.